#ifndef SORT_H
#define SORT_H

#include <stdlib.h>
#include <stddef.h>
void print_array(const int *array, size_t size);
void sort(int *array, size_t size);
int maximum(int *array, int size);
int significant_digit(int num, int n);
void sort_digit(int *array, int ctn, int size)
#endif /* SORT_H */